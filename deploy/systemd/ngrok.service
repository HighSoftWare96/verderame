[Unit]
Description=ngrok
After=network.target

[Service]
ExecStart=/opt/ngrok/ngrok http --domain=$NGROK_STATIC_DOMAIN 5000
ExecReload=/bin/kill -HUP $MAINPID
KillMode=process
IgnoreSIGPIPE=true
Restart=always
RestartSec=3
Type=simple

[Install]
WantedBy=multi-user.target